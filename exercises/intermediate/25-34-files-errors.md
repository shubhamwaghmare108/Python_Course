# Curated Lab Set

## Lab 1: Read text file

**Objective:** Build a working solution for **Read text file** using the concepts already taught in the course.

**Task:** Create a small, realistic example. Include normal input, one edge case, and a short explanation of your approach.

**Acceptance criteria:** The program runs without uncaught errors for valid input, uses meaningful names, and produces a result that can be checked manually.

**Challenge:** Add one improvement such as validation, reusable functions, or a more efficient implementation.

**Interview question:** Explain the key Python/data concept used here and when you would choose it over an alternative.

---

## Lab 2: Write text file

**Objective:** Build a working solution for **Write text file** using the concepts already taught in the course.

**Task:** Create a small, realistic example. Include normal input, one edge case, and a short explanation of your approach.

**Acceptance criteria:** The program runs without uncaught errors for valid input, uses meaningful names, and produces a result that can be checked manually.

**Challenge:** Add one improvement such as validation, reusable functions, or a more efficient implementation.

**Interview question:** Explain the key Python/data concept used here and when you would choose it over an alternative.

---

## Lab 3: Append safely

**Objective:** Build a working solution for **Append safely** using the concepts already taught in the course.

**Task:** Create a small, realistic example. Include normal input, one edge case, and a short explanation of your approach.

**Acceptance criteria:** The program runs without uncaught errors for valid input, uses meaningful names, and produces a result that can be checked manually.

**Challenge:** Add one improvement such as validation, reusable functions, or a more efficient implementation.

**Interview question:** Explain the key Python/data concept used here and when you would choose it over an alternative.

---

## Lab 4: Count file statistics

**Objective:** Build a working solution for **Count file statistics** using the concepts already taught in the course.

**Task:** Create a small, realistic example. Include normal input, one edge case, and a short explanation of your approach.

**Acceptance criteria:** The program runs without uncaught errors for valid input, uses meaningful names, and produces a result that can be checked manually.

**Challenge:** Add one improvement such as validation, reusable functions, or a more efficient implementation.

**Interview question:** Explain the key Python/data concept used here and when you would choose it over an alternative.

---

## Lab 5: CSV reader

**Objective:** Build a working solution for **CSV reader** using the concepts already taught in the course.

**Task:** Create a small, realistic example. Include normal input, one edge case, and a short explanation of your approach.

**Acceptance criteria:** The program runs without uncaught errors for valid input, uses meaningful names, and produces a result that can be checked manually.

**Challenge:** Add one improvement such as validation, reusable functions, or a more efficient implementation.

**Interview question:** Explain the key Python/data concept used here and when you would choose it over an alternative.

---

## Lab 6: JSON reader

**Objective:** Build a working solution for **JSON reader** using the concepts already taught in the course.

**Task:** Create a small, realistic example. Include normal input, one edge case, and a short explanation of your approach.

**Acceptance criteria:** The program runs without uncaught errors for valid input, uses meaningful names, and produces a result that can be checked manually.

**Challenge:** Add one improvement such as validation, reusable functions, or a more efficient implementation.

**Interview question:** Explain the key Python/data concept used here and when you would choose it over an alternative.

---

## Lab 7: Context manager

**Objective:** Build a working solution for **Context manager** using the concepts already taught in the course.

**Task:** Create a small, realistic example. Include normal input, one edge case, and a short explanation of your approach.

**Acceptance criteria:** The program runs without uncaught errors for valid input, uses meaningful names, and produces a result that can be checked manually.

**Challenge:** Add one improvement such as validation, reusable functions, or a more efficient implementation.

**Interview question:** Explain the key Python/data concept used here and when you would choose it over an alternative.

---

## Lab 8: Custom exception

**Objective:** Build a working solution for **Custom exception** using the concepts already taught in the course.

**Task:** Create a small, realistic example. Include normal input, one edge case, and a short explanation of your approach.

**Acceptance criteria:** The program runs without uncaught errors for valid input, uses meaningful names, and produces a result that can be checked manually.

**Challenge:** Add one improvement such as validation, reusable functions, or a more efficient implementation.

**Interview question:** Explain the key Python/data concept used here and when you would choose it over an alternative.

---

## Lab 9: Exception hierarchy

**Objective:** Build a working solution for **Exception hierarchy** using the concepts already taught in the course.

**Task:** Create a small, realistic example. Include normal input, one edge case, and a short explanation of your approach.

**Acceptance criteria:** The program runs without uncaught errors for valid input, uses meaningful names, and produces a result that can be checked manually.

**Challenge:** Add one improvement such as validation, reusable functions, or a more efficient implementation.

**Interview question:** Explain the key Python/data concept used here and when you would choose it over an alternative.

---

## Lab 10: Logging errors

**Objective:** Build a working solution for **Logging errors** using the concepts already taught in the course.

**Task:** Create a small, realistic example. Include normal input, one edge case, and a short explanation of your approach.

**Acceptance criteria:** The program runs without uncaught errors for valid input, uses meaningful names, and produces a result that can be checked manually.

**Challenge:** Add one improvement such as validation, reusable functions, or a more efficient implementation.

**Interview question:** Explain the key Python/data concept used here and when you would choose it over an alternative.

---

