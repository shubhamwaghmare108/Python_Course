# Curated Lab Set

## Lab 1: Read text file

**Objective:** Practice **Read text file** by completing a small, testable task rather than only writing a demonstration.

**Task:** Create a temporary/local test file or use a repo-relative `datasets/` path; do not use an absolute Windows path. Build a solution specifically for **Read text file**. Document the input you used and the exact output you obtained. Show the normal result and demonstrate how the program behaves when the file/data is missing or invalid.

**Acceptance criteria:**
- Required behavior works for the stated example.
- Include at least 3 test cases: normal, boundary, and invalid/empty input where applicable.
- Output is deterministic and easy to verify manually.
- Use meaningful names and keep the solution scoped to the concept being practiced.
- Do not use machine-specific paths or hard-coded credentials.

**Challenge:** Add one extension that increases realism without changing the core requirement (for example validation, reusable functions, sorting, reporting, or a second input case).

**Interview question:** Why is **Read text file** useful, and what common mistake would you watch for when implementing it?

---

## Lab 2: Write text file

**Objective:** Practice **Write text file** by completing a small, testable task rather than only writing a demonstration.

**Task:** Create a temporary/local test file or use a repo-relative `datasets/` path; do not use an absolute Windows path. Build a solution specifically for **Write text file**. Document the input you used and the exact output you obtained. Show the normal result and demonstrate how the program behaves when the file/data is missing or invalid.

**Acceptance criteria:**
- Required behavior works for the stated example.
- Include at least 3 test cases: normal, boundary, and invalid/empty input where applicable.
- Output is deterministic and easy to verify manually.
- Use meaningful names and keep the solution scoped to the concept being practiced.
- Do not use machine-specific paths or hard-coded credentials.

**Challenge:** Add one extension that increases realism without changing the core requirement (for example validation, reusable functions, sorting, reporting, or a second input case).

**Interview question:** Why is **Write text file** useful, and what common mistake would you watch for when implementing it?

---

## Lab 3: Append safely

**Objective:** Practice **Append safely** by completing a small, testable task rather than only writing a demonstration.

**Task:** Create a temporary/local test file or use a repo-relative `datasets/` path; do not use an absolute Windows path. Build a solution specifically for **Append safely**. Document the input you used and the exact output you obtained. Show the normal result and demonstrate how the program behaves when the file/data is missing or invalid.

**Acceptance criteria:**
- Required behavior works for the stated example.
- Include at least 3 test cases: normal, boundary, and invalid/empty input where applicable.
- Output is deterministic and easy to verify manually.
- Use meaningful names and keep the solution scoped to the concept being practiced.
- Do not use machine-specific paths or hard-coded credentials.

**Challenge:** Add one extension that increases realism without changing the core requirement (for example validation, reusable functions, sorting, reporting, or a second input case).

**Interview question:** Why is **Append safely** useful, and what common mistake would you watch for when implementing it?

---

## Lab 4: Count file statistics

**Objective:** Practice **Count file statistics** by completing a small, testable task rather than only writing a demonstration.

**Task:** Create a temporary/local test file or use a repo-relative `datasets/` path; do not use an absolute Windows path. Build a solution specifically for **Count file statistics**. Document the input you used and the exact output you obtained. Show the normal result and demonstrate how the program behaves when the file/data is missing or invalid.

**Acceptance criteria:**
- Required behavior works for the stated example.
- Include at least 3 test cases: normal, boundary, and invalid/empty input where applicable.
- Output is deterministic and easy to verify manually.
- Use meaningful names and keep the solution scoped to the concept being practiced.
- Do not use machine-specific paths or hard-coded credentials.

**Challenge:** Add one extension that increases realism without changing the core requirement (for example validation, reusable functions, sorting, reporting, or a second input case).

**Interview question:** Why is **Count file statistics** useful, and what common mistake would you watch for when implementing it?

---

## Lab 5: CSV reader

**Objective:** Practice **CSV reader** by completing a small, testable task rather than only writing a demonstration.

**Task:** Create a temporary/local test file or use a repo-relative `datasets/` path; do not use an absolute Windows path. Build a solution specifically for **CSV reader**. Document the input you used and the exact output you obtained. Show the normal result and demonstrate how the program behaves when the file/data is missing or invalid.

**Acceptance criteria:**
- Required behavior works for the stated example.
- Include at least 3 test cases: normal, boundary, and invalid/empty input where applicable.
- Output is deterministic and easy to verify manually.
- Use meaningful names and keep the solution scoped to the concept being practiced.
- Do not use machine-specific paths or hard-coded credentials.

**Challenge:** Add one extension that increases realism without changing the core requirement (for example validation, reusable functions, sorting, reporting, or a second input case).

**Interview question:** Why is **CSV reader** useful, and what common mistake would you watch for when implementing it?

---

## Lab 6: JSON reader

**Objective:** Practice **JSON reader** by completing a small, testable task rather than only writing a demonstration.

**Task:** Create a temporary/local test file or use a repo-relative `datasets/` path; do not use an absolute Windows path. Build a solution specifically for **JSON reader**. Document the input you used and the exact output you obtained. Show the normal result and demonstrate how the program behaves when the file/data is missing or invalid.

**Acceptance criteria:**
- Required behavior works for the stated example.
- Include at least 3 test cases: normal, boundary, and invalid/empty input where applicable.
- Output is deterministic and easy to verify manually.
- Use meaningful names and keep the solution scoped to the concept being practiced.
- Do not use machine-specific paths or hard-coded credentials.

**Challenge:** Add one extension that increases realism without changing the core requirement (for example validation, reusable functions, sorting, reporting, or a second input case).

**Interview question:** Why is **JSON reader** useful, and what common mistake would you watch for when implementing it?

---

## Lab 7: Context manager

**Objective:** Practice **Context manager** by completing a small, testable task rather than only writing a demonstration.

**Task:** Create a temporary/local test file or use a repo-relative `datasets/` path; do not use an absolute Windows path. Build a solution specifically for **Context manager**. Document the input you used and the exact output you obtained. Show the normal result and demonstrate how the program behaves when the file/data is missing or invalid.

**Acceptance criteria:**
- Required behavior works for the stated example.
- Include at least 3 test cases: normal, boundary, and invalid/empty input where applicable.
- Output is deterministic and easy to verify manually.
- Use meaningful names and keep the solution scoped to the concept being practiced.
- Do not use machine-specific paths or hard-coded credentials.

**Challenge:** Add one extension that increases realism without changing the core requirement (for example validation, reusable functions, sorting, reporting, or a second input case).

**Interview question:** Why is **Context manager** useful, and what common mistake would you watch for when implementing it?

---

## Lab 8: Custom exception

**Objective:** Practice **Custom exception** by completing a small, testable task rather than only writing a demonstration.

**Task:** Create a temporary/local test file or use a repo-relative `datasets/` path; do not use an absolute Windows path. Build a solution specifically for **Custom exception**. Document the input you used and the exact output you obtained. Show the normal result and demonstrate how the program behaves when the file/data is missing or invalid.

**Acceptance criteria:**
- Required behavior works for the stated example.
- Include at least 3 test cases: normal, boundary, and invalid/empty input where applicable.
- Output is deterministic and easy to verify manually.
- Use meaningful names and keep the solution scoped to the concept being practiced.
- Do not use machine-specific paths or hard-coded credentials.

**Challenge:** Add one extension that increases realism without changing the core requirement (for example validation, reusable functions, sorting, reporting, or a second input case).

**Interview question:** Why is **Custom exception** useful, and what common mistake would you watch for when implementing it?

---

## Lab 9: Exception hierarchy

**Objective:** Practice **Exception hierarchy** by completing a small, testable task rather than only writing a demonstration.

**Task:** Create a temporary/local test file or use a repo-relative `datasets/` path; do not use an absolute Windows path. Build a solution specifically for **Exception hierarchy**. Document the input you used and the exact output you obtained. Show the normal result and demonstrate how the program behaves when the file/data is missing or invalid.

**Acceptance criteria:**
- Required behavior works for the stated example.
- Include at least 3 test cases: normal, boundary, and invalid/empty input where applicable.
- Output is deterministic and easy to verify manually.
- Use meaningful names and keep the solution scoped to the concept being practiced.
- Do not use machine-specific paths or hard-coded credentials.

**Challenge:** Add one extension that increases realism without changing the core requirement (for example validation, reusable functions, sorting, reporting, or a second input case).

**Interview question:** Why is **Exception hierarchy** useful, and what common mistake would you watch for when implementing it?

---

## Lab 10: Logging errors

**Objective:** Practice **Logging errors** by completing a small, testable task rather than only writing a demonstration.

**Task:** Create a temporary/local test file or use a repo-relative `datasets/` path; do not use an absolute Windows path. Build a solution specifically for **Logging errors**. Document the input you used and the exact output you obtained. Show the normal result and demonstrate how the program behaves when the file/data is missing or invalid.

**Acceptance criteria:**
- Required behavior works for the stated example.
- Include at least 3 test cases: normal, boundary, and invalid/empty input where applicable.
- Output is deterministic and easy to verify manually.
- Use meaningful names and keep the solution scoped to the concept being practiced.
- Do not use machine-specific paths or hard-coded credentials.

**Challenge:** Add one extension that increases realism without changing the core requirement (for example validation, reusable functions, sorting, reporting, or a second input case).

**Interview question:** Why is **Logging errors** useful, and what common mistake would you watch for when implementing it?

