# Curated Lab Set

## Lab 1: Keyword arguments

**Objective:** Build a working solution for **Keyword arguments** using the concepts already taught in the course.

**Task:** Create a small, realistic example. Include normal input, one edge case, and a short explanation of your approach.

**Acceptance criteria:** The program runs without uncaught errors for valid input, uses meaningful names, and produces a result that can be checked manually.

**Challenge:** Add one improvement such as validation, reusable functions, or a more efficient implementation.

**Interview question:** Explain the key Python/data concept used here and when you would choose it over an alternative.

---

## Lab 2: Variable-length arguments

**Objective:** Build a working solution for **Variable-length arguments** using the concepts already taught in the course.

**Task:** Create a small, realistic example. Include normal input, one edge case, and a short explanation of your approach.

**Acceptance criteria:** The program runs without uncaught errors for valid input, uses meaningful names, and produces a result that can be checked manually.

**Challenge:** Add one improvement such as validation, reusable functions, or a more efficient implementation.

**Interview question:** Explain the key Python/data concept used here and when you would choose it over an alternative.

---

## Lab 3: Recursive factorial

**Objective:** Build a working solution for **Recursive factorial** using the concepts already taught in the course.

**Task:** Create a small, realistic example. Include normal input, one edge case, and a short explanation of your approach.

**Acceptance criteria:** The program runs without uncaught errors for valid input, uses meaningful names, and produces a result that can be checked manually.

**Challenge:** Add one improvement such as validation, reusable functions, or a more efficient implementation.

**Interview question:** Explain the key Python/data concept used here and when you would choose it over an alternative.

---

## Lab 4: Recursive Fibonacci

**Objective:** Build a working solution for **Recursive Fibonacci** using the concepts already taught in the course.

**Task:** Create a small, realistic example. Include normal input, one edge case, and a short explanation of your approach.

**Acceptance criteria:** The program runs without uncaught errors for valid input, uses meaningful names, and produces a result that can be checked manually.

**Challenge:** Add one improvement such as validation, reusable functions, or a more efficient implementation.

**Interview question:** Explain the key Python/data concept used here and when you would choose it over an alternative.

---

## Lab 5: Higher-order function

**Objective:** Build a working solution for **Higher-order function** using the concepts already taught in the course.

**Task:** Create a small, realistic example. Include normal input, one edge case, and a short explanation of your approach.

**Acceptance criteria:** The program runs without uncaught errors for valid input, uses meaningful names, and produces a result that can be checked manually.

**Challenge:** Add one improvement such as validation, reusable functions, or a more efficient implementation.

**Interview question:** Explain the key Python/data concept used here and when you would choose it over an alternative.

---

## Lab 6: Map/filter practice

**Objective:** Build a working solution for **Map/filter practice** using the concepts already taught in the course.

**Task:** Create a small, realistic example. Include normal input, one edge case, and a short explanation of your approach.

**Acceptance criteria:** The program runs without uncaught errors for valid input, uses meaningful names, and produces a result that can be checked manually.

**Challenge:** Add one improvement such as validation, reusable functions, or a more efficient implementation.

**Interview question:** Explain the key Python/data concept used here and when you would choose it over an alternative.

---

## Lab 7: Reduce practice

**Objective:** Build a working solution for **Reduce practice** using the concepts already taught in the course.

**Task:** Create a small, realistic example. Include normal input, one edge case, and a short explanation of your approach.

**Acceptance criteria:** The program runs without uncaught errors for valid input, uses meaningful names, and produces a result that can be checked manually.

**Challenge:** Add one improvement such as validation, reusable functions, or a more efficient implementation.

**Interview question:** Explain the key Python/data concept used here and when you would choose it over an alternative.

---

## Lab 8: Function composition

**Objective:** Build a working solution for **Function composition** using the concepts already taught in the course.

**Task:** Create a small, realistic example. Include normal input, one edge case, and a short explanation of your approach.

**Acceptance criteria:** The program runs without uncaught errors for valid input, uses meaningful names, and produces a result that can be checked manually.

**Challenge:** Add one improvement such as validation, reusable functions, or a more efficient implementation.

**Interview question:** Explain the key Python/data concept used here and when you would choose it over an alternative.

---

## Lab 9: Closure

**Objective:** Build a working solution for **Closure** using the concepts already taught in the course.

**Task:** Create a small, realistic example. Include normal input, one edge case, and a short explanation of your approach.

**Acceptance criteria:** The program runs without uncaught errors for valid input, uses meaningful names, and produces a result that can be checked manually.

**Challenge:** Add one improvement such as validation, reusable functions, or a more efficient implementation.

**Interview question:** Explain the key Python/data concept used here and when you would choose it over an alternative.

---

## Lab 10: Docstring and annotations

**Objective:** Build a working solution for **Docstring and annotations** using the concepts already taught in the course.

**Task:** Create a small, realistic example. Include normal input, one edge case, and a short explanation of your approach.

**Acceptance criteria:** The program runs without uncaught errors for valid input, uses meaningful names, and produces a result that can be checked manually.

**Challenge:** Add one improvement such as validation, reusable functions, or a more efficient implementation.

**Interview question:** Explain the key Python/data concept used here and when you would choose it over an alternative.

---

