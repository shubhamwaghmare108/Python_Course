# Curated Lab Set

## Lab 1: Class attributes

**Objective:** Build a working solution for **Class attributes** using the concepts already taught in the course.

**Task:** Create a small, realistic example. Include normal input, one edge case, and a short explanation of your approach.

**Acceptance criteria:** The program runs without uncaught errors for valid input, uses meaningful names, and produces a result that can be checked manually.

**Challenge:** Add one improvement such as validation, reusable functions, or a more efficient implementation.

**Interview question:** Explain the key Python/data concept used here and when you would choose it over an alternative.

---

## Lab 2: Instance methods

**Objective:** Build a working solution for **Instance methods** using the concepts already taught in the course.

**Task:** Create a small, realistic example. Include normal input, one edge case, and a short explanation of your approach.

**Acceptance criteria:** The program runs without uncaught errors for valid input, uses meaningful names, and produces a result that can be checked manually.

**Challenge:** Add one improvement such as validation, reusable functions, or a more efficient implementation.

**Interview question:** Explain the key Python/data concept used here and when you would choose it over an alternative.

---

## Lab 3: Class methods

**Objective:** Build a working solution for **Class methods** using the concepts already taught in the course.

**Task:** Create a small, realistic example. Include normal input, one edge case, and a short explanation of your approach.

**Acceptance criteria:** The program runs without uncaught errors for valid input, uses meaningful names, and produces a result that can be checked manually.

**Challenge:** Add one improvement such as validation, reusable functions, or a more efficient implementation.

**Interview question:** Explain the key Python/data concept used here and when you would choose it over an alternative.

---

## Lab 4: Static methods

**Objective:** Build a working solution for **Static methods** using the concepts already taught in the course.

**Task:** Create a small, realistic example. Include normal input, one edge case, and a short explanation of your approach.

**Acceptance criteria:** The program runs without uncaught errors for valid input, uses meaningful names, and produces a result that can be checked manually.

**Challenge:** Add one improvement such as validation, reusable functions, or a more efficient implementation.

**Interview question:** Explain the key Python/data concept used here and when you would choose it over an alternative.

---

## Lab 5: Inheritance

**Objective:** Build a working solution for **Inheritance** using the concepts already taught in the course.

**Task:** Create a small, realistic example. Include normal input, one edge case, and a short explanation of your approach.

**Acceptance criteria:** The program runs without uncaught errors for valid input, uses meaningful names, and produces a result that can be checked manually.

**Challenge:** Add one improvement such as validation, reusable functions, or a more efficient implementation.

**Interview question:** Explain the key Python/data concept used here and when you would choose it over an alternative.

---

## Lab 6: Method overriding

**Objective:** Build a working solution for **Method overriding** using the concepts already taught in the course.

**Task:** Create a small, realistic example. Include normal input, one edge case, and a short explanation of your approach.

**Acceptance criteria:** The program runs without uncaught errors for valid input, uses meaningful names, and produces a result that can be checked manually.

**Challenge:** Add one improvement such as validation, reusable functions, or a more efficient implementation.

**Interview question:** Explain the key Python/data concept used here and when you would choose it over an alternative.

---

## Lab 7: Composition

**Objective:** Build a working solution for **Composition** using the concepts already taught in the course.

**Task:** Create a small, realistic example. Include normal input, one edge case, and a short explanation of your approach.

**Acceptance criteria:** The program runs without uncaught errors for valid input, uses meaningful names, and produces a result that can be checked manually.

**Challenge:** Add one improvement such as validation, reusable functions, or a more efficient implementation.

**Interview question:** Explain the key Python/data concept used here and when you would choose it over an alternative.

---

## Lab 8: Encapsulation

**Objective:** Build a working solution for **Encapsulation** using the concepts already taught in the course.

**Task:** Create a small, realistic example. Include normal input, one edge case, and a short explanation of your approach.

**Acceptance criteria:** The program runs without uncaught errors for valid input, uses meaningful names, and produces a result that can be checked manually.

**Challenge:** Add one improvement such as validation, reusable functions, or a more efficient implementation.

**Interview question:** Explain the key Python/data concept used here and when you would choose it over an alternative.

---

## Lab 9: Property validation

**Objective:** Build a working solution for **Property validation** using the concepts already taught in the course.

**Task:** Create a small, realistic example. Include normal input, one edge case, and a short explanation of your approach.

**Acceptance criteria:** The program runs without uncaught errors for valid input, uses meaningful names, and produces a result that can be checked manually.

**Challenge:** Add one improvement such as validation, reusable functions, or a more efficient implementation.

**Interview question:** Explain the key Python/data concept used here and when you would choose it over an alternative.

---

## Lab 10: Mini banking model

**Objective:** Build a working solution for **Mini banking model** using the concepts already taught in the course.

**Task:** Create a small, realistic example. Include normal input, one edge case, and a short explanation of your approach.

**Acceptance criteria:** The program runs without uncaught errors for valid input, uses meaningful names, and produces a result that can be checked manually.

**Challenge:** Add one improvement such as validation, reusable functions, or a more efficient implementation.

**Interview question:** Explain the key Python/data concept used here and when you would choose it over an alternative.

---

