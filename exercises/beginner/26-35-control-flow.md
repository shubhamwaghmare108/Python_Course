# Curated Lab Set

## Lab 1: FizzBuzz

**Objective:** Practice **FizzBuzz** by completing a small, testable task rather than only writing a demonstration.

**Task:** Use a small, self-contained input example so another student can reproduce your result. Build a solution specifically for **FizzBuzz**. Document the input you used and the exact output you obtained. Show the input, the resulting output, and at least one edge-case result.

**Acceptance criteria:**
- Required behavior works for the stated example.
- Include at least 3 test cases: normal, boundary, and invalid/empty input where applicable.
- Output is deterministic and easy to verify manually.
- Use meaningful names and keep the solution scoped to the concept being practiced.
- Do not use machine-specific paths or hard-coded credentials.

**Challenge:** Add one extension that increases realism without changing the core requirement (for example validation, reusable functions, sorting, reporting, or a second input case).

**Interview question:** Why is **FizzBuzz** useful, and what common mistake would you watch for when implementing it?

---

## Lab 2: Prime check

**Objective:** Practice **Prime check** by completing a small, testable task rather than only writing a demonstration.

**Task:** Use a small, self-contained input example so another student can reproduce your result. Build a solution specifically for **Prime check**. Document the input you used and the exact output you obtained. Show the input, the resulting output, and at least one edge-case result.

**Acceptance criteria:**
- Required behavior works for the stated example.
- Include at least 3 test cases: normal, boundary, and invalid/empty input where applicable.
- Output is deterministic and easy to verify manually.
- Use meaningful names and keep the solution scoped to the concept being practiced.
- Do not use machine-specific paths or hard-coded credentials.

**Challenge:** Add one extension that increases realism without changing the core requirement (for example validation, reusable functions, sorting, reporting, or a second input case).

**Interview question:** Why is **Prime check** useful, and what common mistake would you watch for when implementing it?

---

## Lab 3: Prime numbers

**Objective:** Practice **Prime numbers** by completing a small, testable task rather than only writing a demonstration.

**Task:** Use a small, self-contained input example so another student can reproduce your result. Build a solution specifically for **Prime numbers**. Document the input you used and the exact output you obtained. Show the input, the resulting output, and at least one edge-case result.

**Acceptance criteria:**
- Required behavior works for the stated example.
- Include at least 3 test cases: normal, boundary, and invalid/empty input where applicable.
- Output is deterministic and easy to verify manually.
- Use meaningful names and keep the solution scoped to the concept being practiced.
- Do not use machine-specific paths or hard-coded credentials.

**Challenge:** Add one extension that increases realism without changing the core requirement (for example validation, reusable functions, sorting, reporting, or a second input case).

**Interview question:** Why is **Prime numbers** useful, and what common mistake would you watch for when implementing it?

---

## Lab 4: Factorial

**Objective:** Practice **Factorial** by completing a small, testable task rather than only writing a demonstration.

**Task:** Use a small, self-contained input example so another student can reproduce your result. Build a solution specifically for **Factorial**. Document the input you used and the exact output you obtained. Show the input, the resulting output, and at least one edge-case result.

**Acceptance criteria:**
- Required behavior works for the stated example.
- Include at least 3 test cases: normal, boundary, and invalid/empty input where applicable.
- Output is deterministic and easy to verify manually.
- Use meaningful names and keep the solution scoped to the concept being practiced.
- Do not use machine-specific paths or hard-coded credentials.

**Challenge:** Add one extension that increases realism without changing the core requirement (for example validation, reusable functions, sorting, reporting, or a second input case).

**Interview question:** Why is **Factorial** useful, and what common mistake would you watch for when implementing it?

---

## Lab 5: Fibonacci

**Objective:** Practice **Fibonacci** by completing a small, testable task rather than only writing a demonstration.

**Task:** Use a small, self-contained input example so another student can reproduce your result. Build a solution specifically for **Fibonacci**. Document the input you used and the exact output you obtained. Show the input, the resulting output, and at least one edge-case result.

**Acceptance criteria:**
- Required behavior works for the stated example.
- Include at least 3 test cases: normal, boundary, and invalid/empty input where applicable.
- Output is deterministic and easy to verify manually.
- Use meaningful names and keep the solution scoped to the concept being practiced.
- Do not use machine-specific paths or hard-coded credentials.

**Challenge:** Add one extension that increases realism without changing the core requirement (for example validation, reusable functions, sorting, reporting, or a second input case).

**Interview question:** Why is **Fibonacci** useful, and what common mistake would you watch for when implementing it?

---

## Lab 6: Sum of digits

**Objective:** Practice **Sum of digits** by completing a small, testable task rather than only writing a demonstration.

**Task:** Use a small, self-contained input example so another student can reproduce your result. Build a solution specifically for **Sum of digits**. Document the input you used and the exact output you obtained. Show the input, the resulting output, and at least one edge-case result.

**Acceptance criteria:**
- Required behavior works for the stated example.
- Include at least 3 test cases: normal, boundary, and invalid/empty input where applicable.
- Output is deterministic and easy to verify manually.
- Use meaningful names and keep the solution scoped to the concept being practiced.
- Do not use machine-specific paths or hard-coded credentials.

**Challenge:** Add one extension that increases realism without changing the core requirement (for example validation, reusable functions, sorting, reporting, or a second input case).

**Interview question:** Why is **Sum of digits** useful, and what common mistake would you watch for when implementing it?

---

## Lab 7: Reverse an integer

**Objective:** Practice **Reverse an integer** by completing a small, testable task rather than only writing a demonstration.

**Task:** Use a small, self-contained input example so another student can reproduce your result. Build a solution specifically for **Reverse an integer**. Document the input you used and the exact output you obtained. Show the input, the resulting output, and at least one edge-case result.

**Acceptance criteria:**
- Required behavior works for the stated example.
- Include at least 3 test cases: normal, boundary, and invalid/empty input where applicable.
- Output is deterministic and easy to verify manually.
- Use meaningful names and keep the solution scoped to the concept being practiced.
- Do not use machine-specific paths or hard-coded credentials.

**Challenge:** Add one extension that increases realism without changing the core requirement (for example validation, reusable functions, sorting, reporting, or a second input case).

**Interview question:** Why is **Reverse an integer** useful, and what common mistake would you watch for when implementing it?

---

## Lab 8: Armstrong number

**Objective:** Practice **Armstrong number** by completing a small, testable task rather than only writing a demonstration.

**Task:** Use a small, self-contained input example so another student can reproduce your result. Build a solution specifically for **Armstrong number**. Document the input you used and the exact output you obtained. Show the input, the resulting output, and at least one edge-case result.

**Acceptance criteria:**
- Required behavior works for the stated example.
- Include at least 3 test cases: normal, boundary, and invalid/empty input where applicable.
- Output is deterministic and easy to verify manually.
- Use meaningful names and keep the solution scoped to the concept being practiced.
- Do not use machine-specific paths or hard-coded credentials.

**Challenge:** Add one extension that increases realism without changing the core requirement (for example validation, reusable functions, sorting, reporting, or a second input case).

**Interview question:** Why is **Armstrong number** useful, and what common mistake would you watch for when implementing it?

---

## Lab 9: Menu-driven program

**Objective:** Practice **Menu-driven program** by completing a small, testable task rather than only writing a demonstration.

**Task:** Use a small, self-contained input example so another student can reproduce your result. Build a solution specifically for **Menu-driven program**. Document the input you used and the exact output you obtained. Show the input, the resulting output, and at least one edge-case result.

**Acceptance criteria:**
- Required behavior works for the stated example.
- Include at least 3 test cases: normal, boundary, and invalid/empty input where applicable.
- Output is deterministic and easy to verify manually.
- Use meaningful names and keep the solution scoped to the concept being practiced.
- Do not use machine-specific paths or hard-coded credentials.

**Challenge:** Add one extension that increases realism without changing the core requirement (for example validation, reusable functions, sorting, reporting, or a second input case).

**Interview question:** Why is **Menu-driven program** useful, and what common mistake would you watch for when implementing it?

---

## Lab 10: Input validation loop

**Objective:** Practice **Input validation loop** by completing a small, testable task rather than only writing a demonstration.

**Task:** Use a small, self-contained input example so another student can reproduce your result. Build a solution specifically for **Input validation loop**. Document the input you used and the exact output you obtained. Show the input, the resulting output, and at least one edge-case result.

**Acceptance criteria:**
- Required behavior works for the stated example.
- Include at least 3 test cases: normal, boundary, and invalid/empty input where applicable.
- Output is deterministic and easy to verify manually.
- Use meaningful names and keep the solution scoped to the concept being practiced.
- Do not use machine-specific paths or hard-coded credentials.

**Challenge:** Add one extension that increases realism without changing the core requirement (for example validation, reusable functions, sorting, reporting, or a second input case).

**Interview question:** Why is **Input validation loop** useful, and what common mistake would you watch for when implementing it?

