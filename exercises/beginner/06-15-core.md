# Curated Lab Set

## Lab 6: Arithmetic and precedence

**Objective:** Practice **Arithmetic and precedence** by completing a small, testable task rather than only writing a demonstration.

**Task:** Use a small, self-contained input example so another student can reproduce your result. Build a solution specifically for **Arithmetic and precedence**. Document the input you used and the exact output you obtained. Show the input, the resulting output, and at least one edge-case result.

**Acceptance criteria:**
- Required behavior works for the stated example.
- Include at least 3 test cases: normal, boundary, and invalid/empty input where applicable.
- Output is deterministic and easy to verify manually.
- Use meaningful names and keep the solution scoped to the concept being practiced.
- Do not use machine-specific paths or hard-coded credentials.

**Challenge:** Add one extension that increases realism without changing the core requirement (for example validation, reusable functions, sorting, reporting, or a second input case).

**Interview question:** Why is **Arithmetic and precedence** useful, and what common mistake would you watch for when implementing it?

---

## Lab 7: Type conversion

**Objective:** Practice **Type conversion** by completing a small, testable task rather than only writing a demonstration.

**Task:** Use a small, self-contained input example so another student can reproduce your result. Build a solution specifically for **Type conversion**. Document the input you used and the exact output you obtained. Show the input, the resulting output, and at least one edge-case result.

**Acceptance criteria:**
- Required behavior works for the stated example.
- Include at least 3 test cases: normal, boundary, and invalid/empty input where applicable.
- Output is deterministic and easy to verify manually.
- Use meaningful names and keep the solution scoped to the concept being practiced.
- Do not use machine-specific paths or hard-coded credentials.

**Challenge:** Add one extension that increases realism without changing the core requirement (for example validation, reusable functions, sorting, reporting, or a second input case).

**Interview question:** Why is **Type conversion** useful, and what common mistake would you watch for when implementing it?

---

## Lab 8: Formatted strings

**Objective:** Practice **Formatted strings** by completing a small, testable task rather than only writing a demonstration.

**Task:** Use a small, self-contained input example so another student can reproduce your result. Build a solution specifically for **Formatted strings**. Document the input you used and the exact output you obtained. Show the input, the resulting output, and at least one edge-case result.

**Acceptance criteria:**
- Required behavior works for the stated example.
- Include at least 3 test cases: normal, boundary, and invalid/empty input where applicable.
- Output is deterministic and easy to verify manually.
- Use meaningful names and keep the solution scoped to the concept being practiced.
- Do not use machine-specific paths or hard-coded credentials.

**Challenge:** Add one extension that increases realism without changing the core requirement (for example validation, reusable functions, sorting, reporting, or a second input case).

**Interview question:** Why is **Formatted strings** useful, and what common mistake would you watch for when implementing it?

---

## Lab 9: String methods

**Objective:** Practice **String methods** by completing a small, testable task rather than only writing a demonstration.

**Task:** Use a small, self-contained input example so another student can reproduce your result. Build a solution specifically for **String methods**. Document the input you used and the exact output you obtained. Show the input, the resulting output, and at least one edge-case result.

**Acceptance criteria:**
- Required behavior works for the stated example.
- Include at least 3 test cases: normal, boundary, and invalid/empty input where applicable.
- Output is deterministic and easy to verify manually.
- Use meaningful names and keep the solution scoped to the concept being practiced.
- Do not use machine-specific paths or hard-coded credentials.

**Challenge:** Add one extension that increases realism without changing the core requirement (for example validation, reusable functions, sorting, reporting, or a second input case).

**Interview question:** Why is **String methods** useful, and what common mistake would you watch for when implementing it?

---

## Lab 10: Palindrome check

**Objective:** Practice **Palindrome check** by completing a small, testable task rather than only writing a demonstration.

**Task:** Use a small, self-contained input example so another student can reproduce your result. Build a solution specifically for **Palindrome check**. Document the input you used and the exact output you obtained. Show the input, the resulting output, and at least one edge-case result.

**Acceptance criteria:**
- Required behavior works for the stated example.
- Include at least 3 test cases: normal, boundary, and invalid/empty input where applicable.
- Output is deterministic and easy to verify manually.
- Use meaningful names and keep the solution scoped to the concept being practiced.
- Do not use machine-specific paths or hard-coded credentials.

**Challenge:** Add one extension that increases realism without changing the core requirement (for example validation, reusable functions, sorting, reporting, or a second input case).

**Interview question:** Why is **Palindrome check** useful, and what common mistake would you watch for when implementing it?

---

## Lab 11: Character counting

**Objective:** Practice **Character counting** by completing a small, testable task rather than only writing a demonstration.

**Task:** Use a small, self-contained input example so another student can reproduce your result. Build a solution specifically for **Character counting**. Document the input you used and the exact output you obtained. Show the input, the resulting output, and at least one edge-case result.

**Acceptance criteria:**
- Required behavior works for the stated example.
- Include at least 3 test cases: normal, boundary, and invalid/empty input where applicable.
- Output is deterministic and easy to verify manually.
- Use meaningful names and keep the solution scoped to the concept being practiced.
- Do not use machine-specific paths or hard-coded credentials.

**Challenge:** Add one extension that increases realism without changing the core requirement (for example validation, reusable functions, sorting, reporting, or a second input case).

**Interview question:** Why is **Character counting** useful, and what common mistake would you watch for when implementing it?

---

## Lab 12: Simple calculator

**Objective:** Practice **Simple calculator** by completing a small, testable task rather than only writing a demonstration.

**Task:** Use a small, self-contained input example so another student can reproduce your result. Build a solution specifically for **Simple calculator**. Document the input you used and the exact output you obtained. Show the input, the resulting output, and at least one edge-case result.

**Acceptance criteria:**
- Required behavior works for the stated example.
- Include at least 3 test cases: normal, boundary, and invalid/empty input where applicable.
- Output is deterministic and easy to verify manually.
- Use meaningful names and keep the solution scoped to the concept being practiced.
- Do not use machine-specific paths or hard-coded credentials.

**Challenge:** Add one extension that increases realism without changing the core requirement (for example validation, reusable functions, sorting, reporting, or a second input case).

**Interview question:** Why is **Simple calculator** useful, and what common mistake would you watch for when implementing it?

---

## Lab 13: Age calculator

**Objective:** Practice **Age calculator** by completing a small, testable task rather than only writing a demonstration.

**Task:** Use a small, self-contained input example so another student can reproduce your result. Build a solution specifically for **Age calculator**. Document the input you used and the exact output you obtained. Show the input, the resulting output, and at least one edge-case result.

**Acceptance criteria:**
- Required behavior works for the stated example.
- Include at least 3 test cases: normal, boundary, and invalid/empty input where applicable.
- Output is deterministic and easy to verify manually.
- Use meaningful names and keep the solution scoped to the concept being practiced.
- Do not use machine-specific paths or hard-coded credentials.

**Challenge:** Add one extension that increases realism without changing the core requirement (for example validation, reusable functions, sorting, reporting, or a second input case).

**Interview question:** Why is **Age calculator** useful, and what common mistake would you watch for when implementing it?

---

## Lab 14: Temperature converter

**Objective:** Practice **Temperature converter** by completing a small, testable task rather than only writing a demonstration.

**Task:** Use a small, self-contained input example so another student can reproduce your result. Build a solution specifically for **Temperature converter**. Document the input you used and the exact output you obtained. Show the input, the resulting output, and at least one edge-case result.

**Acceptance criteria:**
- Required behavior works for the stated example.
- Include at least 3 test cases: normal, boundary, and invalid/empty input where applicable.
- Output is deterministic and easy to verify manually.
- Use meaningful names and keep the solution scoped to the concept being practiced.
- Do not use machine-specific paths or hard-coded credentials.

**Challenge:** Add one extension that increases realism without changing the core requirement (for example validation, reusable functions, sorting, reporting, or a second input case).

**Interview question:** Why is **Temperature converter** useful, and what common mistake would you watch for when implementing it?

---

## Lab 15: Number classification

**Objective:** Practice **Number classification** by completing a small, testable task rather than only writing a demonstration.

**Task:** Use a small, self-contained input example so another student can reproduce your result. Build a solution specifically for **Number classification**. Document the input you used and the exact output you obtained. Show the input, the resulting output, and at least one edge-case result.

**Acceptance criteria:**
- Required behavior works for the stated example.
- Include at least 3 test cases: normal, boundary, and invalid/empty input where applicable.
- Output is deterministic and easy to verify manually.
- Use meaningful names and keep the solution scoped to the concept being practiced.
- Do not use machine-specific paths or hard-coded credentials.

**Challenge:** Add one extension that increases realism without changing the core requirement (for example validation, reusable functions, sorting, reporting, or a second input case).

**Interview question:** Why is **Number classification** useful, and what common mistake would you watch for when implementing it?

