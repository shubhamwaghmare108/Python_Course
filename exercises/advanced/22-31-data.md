# Curated Lab Set

## Lab 1: Matplotlib chart selection

**Objective:** Build a working solution for **Matplotlib chart selection** using the concepts already taught in the course.

**Task:** Create a small, realistic example. Include normal input, one edge case, and a short explanation of your approach.

**Acceptance criteria:** The program runs without uncaught errors for valid input, uses meaningful names, and produces a result that can be checked manually.

**Challenge:** Add one improvement such as validation, reusable functions, or a more efficient implementation.

**Interview question:** Explain the key Python/data concept used here and when you would choose it over an alternative.

---

## Lab 2: Subplots dashboard

**Objective:** Build a working solution for **Subplots dashboard** using the concepts already taught in the course.

**Task:** Create a small, realistic example. Include normal input, one edge case, and a short explanation of your approach.

**Acceptance criteria:** The program runs without uncaught errors for valid input, uses meaningful names, and produces a result that can be checked manually.

**Challenge:** Add one improvement such as validation, reusable functions, or a more efficient implementation.

**Interview question:** Explain the key Python/data concept used here and when you would choose it over an alternative.

---

## Lab 3: Seaborn categorical analysis

**Objective:** Build a working solution for **Seaborn categorical analysis** using the concepts already taught in the course.

**Task:** Create a small, realistic example. Include normal input, one edge case, and a short explanation of your approach.

**Acceptance criteria:** The program runs without uncaught errors for valid input, uses meaningful names, and produces a result that can be checked manually.

**Challenge:** Add one improvement such as validation, reusable functions, or a more efficient implementation.

**Interview question:** Explain the key Python/data concept used here and when you would choose it over an alternative.

---

## Lab 4: Plotly interactive chart

**Objective:** Build a working solution for **Plotly interactive chart** using the concepts already taught in the course.

**Task:** Create a small, realistic example. Include normal input, one edge case, and a short explanation of your approach.

**Acceptance criteria:** The program runs without uncaught errors for valid input, uses meaningful names, and produces a result that can be checked manually.

**Challenge:** Add one improvement such as validation, reusable functions, or a more efficient implementation.

**Interview question:** Explain the key Python/data concept used here and when you would choose it over an alternative.

---

## Lab 5: EDA checklist

**Objective:** Build a working solution for **EDA checklist** using the concepts already taught in the course.

**Task:** Create a small, realistic example. Include normal input, one edge case, and a short explanation of your approach.

**Acceptance criteria:** The program runs without uncaught errors for valid input, uses meaningful names, and produces a result that can be checked manually.

**Challenge:** Add one improvement such as validation, reusable functions, or a more efficient implementation.

**Interview question:** Explain the key Python/data concept used here and when you would choose it over an alternative.

---

## Lab 6: SQL SELECT from Python

**Objective:** Build a working solution for **SQL SELECT from Python** using the concepts already taught in the course.

**Task:** Create a small, realistic example. Include normal input, one edge case, and a short explanation of your approach.

**Acceptance criteria:** The program runs without uncaught errors for valid input, uses meaningful names, and produces a result that can be checked manually.

**Challenge:** Add one improvement such as validation, reusable functions, or a more efficient implementation.

**Interview question:** Explain the key Python/data concept used here and when you would choose it over an alternative.

---

## Lab 7: Parameterized SQL

**Objective:** Build a working solution for **Parameterized SQL** using the concepts already taught in the course.

**Task:** Create a small, realistic example. Include normal input, one edge case, and a short explanation of your approach.

**Acceptance criteria:** The program runs without uncaught errors for valid input, uses meaningful names, and produces a result that can be checked manually.

**Challenge:** Add one improvement such as validation, reusable functions, or a more efficient implementation.

**Interview question:** Explain the key Python/data concept used here and when you would choose it over an alternative.

---

## Lab 8: Transaction handling

**Objective:** Build a working solution for **Transaction handling** using the concepts already taught in the course.

**Task:** Create a small, realistic example. Include normal input, one edge case, and a short explanation of your approach.

**Acceptance criteria:** The program runs without uncaught errors for valid input, uses meaningful names, and produces a result that can be checked manually.

**Challenge:** Add one improvement such as validation, reusable functions, or a more efficient implementation.

**Interview question:** Explain the key Python/data concept used here and when you would choose it over an alternative.

---

## Lab 9: PySpark DataFrame operations

**Objective:** Build a working solution for **PySpark DataFrame operations** using the concepts already taught in the course.

**Task:** Create a small, realistic example. Include normal input, one edge case, and a short explanation of your approach.

**Acceptance criteria:** The program runs without uncaught errors for valid input, uses meaningful names, and produces a result that can be checked manually.

**Challenge:** Add one improvement such as validation, reusable functions, or a more efficient implementation.

**Interview question:** Explain the key Python/data concept used here and when you would choose it over an alternative.

---

## Lab 10: End-to-end analytics mini project

**Objective:** Build a working solution for **End-to-end analytics mini project** using the concepts already taught in the course.

**Task:** Create a small, realistic example. Include normal input, one edge case, and a short explanation of your approach.

**Acceptance criteria:** The program runs without uncaught errors for valid input, uses meaningful names, and produces a result that can be checked manually.

**Challenge:** Add one improvement such as validation, reusable functions, or a more efficient implementation.

**Interview question:** Explain the key Python/data concept used here and when you would choose it over an alternative.

---

