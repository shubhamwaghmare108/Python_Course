# Curated Lab Set

## Lab 1: Matplotlib chart selection

**Objective:** Practice **Matplotlib chart selection** by completing a small, testable task rather than only writing a demonstration.

**Task:** Use `datasets/titanic_demo.csv` or a small deterministic DataFrame. Build a solution specifically for **Matplotlib chart selection**. Document the input you used and the exact output you obtained. Produce a labeled chart and state why that chart type matches the question.

**Acceptance criteria:**
- Required behavior works for the stated example.
- Include at least 3 test cases: normal, boundary, and invalid/empty input where applicable.
- Output is deterministic and easy to verify manually.
- Use meaningful names and keep the solution scoped to the concept being practiced.
- Do not use machine-specific paths or hard-coded credentials.

**Challenge:** Add one extension that increases realism without changing the core requirement (for example validation, reusable functions, sorting, reporting, or a second input case).

**Interview question:** Why is **Matplotlib chart selection** useful, and what common mistake would you watch for when implementing it?

---

## Lab 2: Subplots dashboard

**Objective:** Practice **Subplots dashboard** by completing a small, testable task rather than only writing a demonstration.

**Task:** Use `datasets/titanic_demo.csv` or a small deterministic DataFrame. Build a solution specifically for **Subplots dashboard**. Document the input you used and the exact output you obtained. Produce a labeled chart and state why that chart type matches the question.

**Acceptance criteria:**
- Required behavior works for the stated example.
- Include at least 3 test cases: normal, boundary, and invalid/empty input where applicable.
- Output is deterministic and easy to verify manually.
- Use meaningful names and keep the solution scoped to the concept being practiced.
- Do not use machine-specific paths or hard-coded credentials.

**Challenge:** Add one extension that increases realism without changing the core requirement (for example validation, reusable functions, sorting, reporting, or a second input case).

**Interview question:** Why is **Subplots dashboard** useful, and what common mistake would you watch for when implementing it?

---

## Lab 3: Seaborn categorical analysis

**Objective:** Practice **Seaborn categorical analysis** by completing a small, testable task rather than only writing a demonstration.

**Task:** Use `datasets/titanic_demo.csv` or a small deterministic DataFrame. Build a solution specifically for **Seaborn categorical analysis**. Document the input you used and the exact output you obtained. Produce a labeled chart and state why that chart type matches the question.

**Acceptance criteria:**
- Required behavior works for the stated example.
- Include at least 3 test cases: normal, boundary, and invalid/empty input where applicable.
- Output is deterministic and easy to verify manually.
- Use meaningful names and keep the solution scoped to the concept being practiced.
- Do not use machine-specific paths or hard-coded credentials.

**Challenge:** Add one extension that increases realism without changing the core requirement (for example validation, reusable functions, sorting, reporting, or a second input case).

**Interview question:** Why is **Seaborn categorical analysis** useful, and what common mistake would you watch for when implementing it?

---

## Lab 4: Plotly interactive chart

**Objective:** Practice **Plotly interactive chart** by completing a small, testable task rather than only writing a demonstration.

**Task:** Use `datasets/titanic_demo.csv` or a small deterministic DataFrame. Build a solution specifically for **Plotly interactive chart**. Document the input you used and the exact output you obtained. Produce a labeled chart and state why that chart type matches the question.

**Acceptance criteria:**
- Required behavior works for the stated example.
- Include at least 3 test cases: normal, boundary, and invalid/empty input where applicable.
- Output is deterministic and easy to verify manually.
- Use meaningful names and keep the solution scoped to the concept being practiced.
- Do not use machine-specific paths or hard-coded credentials.

**Challenge:** Add one extension that increases realism without changing the core requirement (for example validation, reusable functions, sorting, reporting, or a second input case).

**Interview question:** Why is **Plotly interactive chart** useful, and what common mistake would you watch for when implementing it?

---

## Lab 5: EDA checklist

**Objective:** Practice **EDA checklist** by completing a small, testable task rather than only writing a demonstration.

**Task:** Use `datasets/titanic_demo.csv` or a small deterministic DataFrame. Build a solution specifically for **EDA checklist**. Document the input you used and the exact output you obtained. Report the key result plus one interpretation supported by the data.

**Acceptance criteria:**
- Required behavior works for the stated example.
- Include at least 3 test cases: normal, boundary, and invalid/empty input where applicable.
- Output is deterministic and easy to verify manually.
- Use meaningful names and keep the solution scoped to the concept being practiced.
- Do not use machine-specific paths or hard-coded credentials.

**Challenge:** Add one extension that increases realism without changing the core requirement (for example validation, reusable functions, sorting, reporting, or a second input case).

**Interview question:** Why is **EDA checklist** useful, and what common mistake would you watch for when implementing it?

---

## Lab 6: SQL SELECT from Python

**Objective:** Practice **SQL SELECT from Python** by completing a small, testable task rather than only writing a demonstration.

**Task:** Use environment variables for database settings; never put credentials in the notebook or script. Build a solution specifically for **SQL SELECT from Python**. Document the input you used and the exact output you obtained. Report the key result plus one interpretation supported by the data.

**Acceptance criteria:**
- Required behavior works for the stated example.
- Include at least 3 test cases: normal, boundary, and invalid/empty input where applicable.
- Output is deterministic and easy to verify manually.
- Use meaningful names and keep the solution scoped to the concept being practiced.
- Do not use machine-specific paths or hard-coded credentials.

**Challenge:** Add one extension that increases realism without changing the core requirement (for example validation, reusable functions, sorting, reporting, or a second input case).

**Interview question:** Why is **SQL SELECT from Python** useful, and what common mistake would you watch for when implementing it?

---

## Lab 7: Parameterized SQL

**Objective:** Practice **Parameterized SQL** by completing a small, testable task rather than only writing a demonstration.

**Task:** Use environment variables for database settings; never put credentials in the notebook or script. Build a solution specifically for **Parameterized SQL**. Document the input you used and the exact output you obtained. Report the key result plus one interpretation supported by the data.

**Acceptance criteria:**
- Required behavior works for the stated example.
- Include at least 3 test cases: normal, boundary, and invalid/empty input where applicable.
- Output is deterministic and easy to verify manually.
- Use meaningful names and keep the solution scoped to the concept being practiced.
- Do not use machine-specific paths or hard-coded credentials.

**Challenge:** Add one extension that increases realism without changing the core requirement (for example validation, reusable functions, sorting, reporting, or a second input case).

**Interview question:** Why is **Parameterized SQL** useful, and what common mistake would you watch for when implementing it?

---

## Lab 8: Transaction handling

**Objective:** Practice **Transaction handling** by completing a small, testable task rather than only writing a demonstration.

**Task:** Use environment variables for database settings; never put credentials in the notebook or script. Build a solution specifically for **Transaction handling**. Document the input you used and the exact output you obtained. Report the key result plus one interpretation supported by the data.

**Acceptance criteria:**
- Required behavior works for the stated example.
- Include at least 3 test cases: normal, boundary, and invalid/empty input where applicable.
- Output is deterministic and easy to verify manually.
- Use meaningful names and keep the solution scoped to the concept being practiced.
- Do not use machine-specific paths or hard-coded credentials.

**Challenge:** Add one extension that increases realism without changing the core requirement (for example validation, reusable functions, sorting, reporting, or a second input case).

**Interview question:** Why is **Transaction handling** useful, and what common mistake would you watch for when implementing it?

---

## Lab 9: PySpark DataFrame operations

**Objective:** Practice **PySpark DataFrame operations** by completing a small, testable task rather than only writing a demonstration.

**Task:** Use `datasets/titanic_demo.csv` or a small deterministic DataFrame. Build a solution specifically for **PySpark DataFrame operations**. Document the input you used and the exact output you obtained. Report the key result plus one interpretation supported by the data.

**Acceptance criteria:**
- Required behavior works for the stated example.
- Include at least 3 test cases: normal, boundary, and invalid/empty input where applicable.
- Output is deterministic and easy to verify manually.
- Use meaningful names and keep the solution scoped to the concept being practiced.
- Do not use machine-specific paths or hard-coded credentials.

**Challenge:** Add one extension that increases realism without changing the core requirement (for example validation, reusable functions, sorting, reporting, or a second input case).

**Interview question:** Why is **PySpark DataFrame operations** useful, and what common mistake would you watch for when implementing it?

---

## Lab 10: End-to-end analytics mini project

**Objective:** Practice **End-to-end analytics mini project** by completing a small, testable task rather than only writing a demonstration.

**Task:** Use `datasets/titanic_demo.csv` or a small deterministic DataFrame. Build a solution specifically for **End-to-end analytics mini project**. Document the input you used and the exact output you obtained. Report the key result plus one interpretation supported by the data.

**Acceptance criteria:**
- Required behavior works for the stated example.
- Include at least 3 test cases: normal, boundary, and invalid/empty input where applicable.
- Output is deterministic and easy to verify manually.
- Use meaningful names and keep the solution scoped to the concept being practiced.
- Do not use machine-specific paths or hard-coded credentials.

**Challenge:** Add one extension that increases realism without changing the core requirement (for example validation, reusable functions, sorting, reporting, or a second input case).

**Interview question:** Why is **End-to-end analytics mini project** useful, and what common mistake would you watch for when implementing it?

